[defaults]
inventory = hosts
private_key_file = ssh/vagrant
hash_behaviour = merge

[ssh_connection]
pipelining = True
ssh_args = -C -o ControlMaster=auto -o ControlPersist=60s -o UserKnownHostsFile=ssh/known_hosts
